; Да се дефинира процедура (nth lst n), която връща елемента на позиция n, броейки от 0, в списъка l. Същото като вграденото list-ref.

(define (nth lst n)
  (if (= n 0) (car lst)
      (nth (cdr lst) (- n 1)))
  )